<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqupt.yyzx.mapper.UserMapper">

    <!-- 用户结果映射 -->
    <resultMap id="UserResultMap" type="com.cqupt.yyzx.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="roleId" column="role_id"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE username = #{username}
    </select>

    <!-- 根据用户名和密码查询用户 -->
    <select id="findByUsernameAndPassword" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE username = #{username} AND password = #{password}
    </select>

    <!-- 插入新用户 -->
    <insert id="insert" parameterType="com.cqupt.yyzx.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, real_name, phone, email, role_id, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{email}, #{roleId}, #{status}, NOW(), NOW())
    </insert>

    <!-- 更新最后登录时间 -->
    <update id="updateLastLoginTime">
        UPDATE users
        SET last_login_time = NOW(), updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>